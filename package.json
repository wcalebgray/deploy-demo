{
  "name": "deploy-demo",
  "version": "0.0.1",
  "description": "a demo to figure out how git and sh work",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "AWS_DEFAULT_PROFILE=augustine bin/deploy.sh",
    "postversion": "git push && git push --tags"
  },
  "author": "Caleb Gray",
  "license": "MIT"
}
